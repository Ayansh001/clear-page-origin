
# Meal Flow Visualizer

A comprehensive nutrition and fitness tracking application built with modern web technologies.

## Technologies Used

This project is built with:

- Vite
- TypeScript
- React
- Shadcn UI
- Tailwind CSS
- Recharts for data visualization
- React Router for navigation
- Local Storage for data persistence

## Getting Started

To run this project locally:

```sh
# Step 1: Clone the repository
git clone <YOUR_GIT_URL>

# Step 2: Navigate to the project directory
cd meal-flow-visualizer

# Step 3: Install the necessary dependencies
npm i

# Step 4: Start the development server
npm run dev
```

## Features

- Nutrition tracking with detailed macronutrient breakdown
- Water intake monitoring with customizable goals
- Workout planning and tracking
- Progress insights and data visualization
- Persistent data storage using browser's local storage
- Form validation and error handling
- Responsive design for all devices
- Optimized performance with code splitting and lazy loading

## Project Structure

The application is organized into modular components:
- `/src/components` - Reusable UI components
- `/src/pages` - Main application pages
- `/src/lib` - Utility functions and types
- `/src/hooks` - Custom React hooks
- `/src/services` - Service functions for data fetching

## Development

### Debug mode
You can activate the debug console by pressing `Ctrl+Shift+I` or `F12`.

### Adding New Features
When adding new features, ensure they follow the existing pattern and maintain responsiveness on all device sizes.
